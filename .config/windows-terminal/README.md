## Windows Terminal settings.json

This file should be copied into the windows terminal settings.json file
to sync settings between machines.

Inside the terminal use `ctrl` + `shift` + `,` (comma) to open the settings
file. Alternatively, click the bottom-left corner button in the settings
menu.
